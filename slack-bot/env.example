# Slack Bot Configuration
# Get these from https://api.slack.com/apps

# ===========================================
# OAuth Configuration (for Public Distribution)
# ===========================================
# Basic Information → App Credentials
SLACK_CLIENT_ID=your-client-id
SLACK_CLIENT_SECRET=your-client-secret
SLACK_SIGNING_SECRET=your-signing-secret

# ===========================================
# Single Workspace Mode (Dev/Testing Only)
# ===========================================
# OAuth & Permissions → Bot User OAuth Token
# Only needed for single-workspace mode (when CLIENT_ID/SECRET not set)
SLACK_BOT_TOKEN=xoxb-your-bot-token

# Socket Mode (Local Dev Only)
# Settings → Socket Mode → Generate App-Level Token with connections:write scope
SLACK_APP_TOKEN=xapp-your-app-token

# ===========================================
# Application Configuration
# ===========================================
# SRE Agent URL (defaults to localhost for dev)
SRE_AGENT_URL=http://localhost:8000

# Config Service URL (for multi-tenant OAuth and integrations)
CONFIG_SERVICE_URL=http://localhost:8080
