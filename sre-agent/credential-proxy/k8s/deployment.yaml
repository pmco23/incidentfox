# Credential Resolver Deployment (Production)
# ext_authz service for credential injection in sandbox pods
# Uses environment-based credentials from K8s Secret
apiVersion: apps/v1
kind: Deployment
metadata:
  name: credential-resolver
  namespace: incidentfox-prod
  labels:
    app: credential-resolver
    component: sre-agent
spec:
  replicas: 2
  selector:
    matchLabels:
      app: credential-resolver
  template:
    metadata:
      labels:
        app: credential-resolver
        component: sre-agent
    spec:
      serviceAccountName: credential-resolver
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
      - name: credential-resolver
        image: 103002841599.dkr.ecr.us-west-2.amazonaws.com/credential-resolver:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8002
          name: http
          protocol: TCP
        env:
        # Use config_service for multi-tenant credential resolution
        - name: CREDENTIAL_SOURCE
          value: "config_service"
        # Config service URL
        - name: CONFIG_SERVICE_URL
          value: "http://config-service-svc.incidentfox-prod.svc.cluster.local:8080"
        # JWT validation mode: "strict" requires valid JWT for all requests
        - name: JWT_MODE
          value: "strict"
        # Shared secret for JWT validation (must match sre-agent server)
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: incidentfox-secrets
              key: jwt-secret
        # AWS region for Secrets Manager (shared API key for free trials)
        - name: AWS_REGION
          value: "us-west-2"
        - name: SHARED_ANTHROPIC_SECRET
          value: "incidentfox/prod/anthropic"
        # Multi-tenant credential flow:
        # - Customer API keys (Anthropic BYOK, Coralogix, Datadog) -> stored in config-service RDS
        # - Shared Anthropic key (for free trials) -> fetched from AWS Secrets Manager
        # - No integration credentials stored in K8s secrets or environment variables
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 256Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 8002
          initialDelaySeconds: 5
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8002
          initialDelaySeconds: 5
          periodSeconds: 5
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities:
            drop:
            - ALL
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchLabels:
                  app: credential-resolver
              topologyKey: kubernetes.io/hostname
